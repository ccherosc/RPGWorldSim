node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store
saves/
*.local.json
history/

# Generated by the publish pipeline. The site is never committed: it is a
# function of the seed, the code and the data files.
# Anchored: an unanchored `site/` would also hide assets/site, which is
# committed source.
/site/
/.publish/
